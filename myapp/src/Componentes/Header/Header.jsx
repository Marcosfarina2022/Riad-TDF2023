import './Header.css';
const Header = ()=>{// una constante apunta a una dirección de memoria y guarda un lugar ahi va a guardar un objeto, si nosotros queremos que el objeto sea juan o lisandro se puede hacer, lo que no se puede hacer es que el objeto pase a ser un string.  
    return (
        <div className='Header'>
        <h3>Header</h3>
        </div>
    )
}
export default Header;